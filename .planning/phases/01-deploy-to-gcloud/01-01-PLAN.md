---
phase: 01-deploy-to-gcloud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - DEPLOYMENT.md
  - backend/src/main/resources/application.yml
autonomous: false
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "Dockerfile exists at repo root and builds a runnable Spring Boot JAR"
    - "application.yml reads PORT env var with fallback to 8080"
    - "STIB API config is preserved in application.yml"
    - ".dockerignore excludes frontend, .git, and build artifacts"
    - "DEPLOYMENT.md documents the full deployment process"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage Docker build for backend"
      contains: "eclipse-temurin"
    - path: ".dockerignore"
      provides: "Build context exclusions"
      contains: "frontend/"
    - path: "DEPLOYMENT.md"
      provides: "Deployment documentation"
      contains: "gcloud run deploy"
    - path: "backend/src/main/resources/application.yml"
      provides: "Spring config with PORT env var"
      contains: "${PORT:8080}"
  key_links:
    - from: "Dockerfile"
      to: "backend/build.gradle"
      via: "gradlew bootJar in build stage"
      pattern: "gradlew bootJar"
    - from: "Dockerfile"
      to: "api-spec/openapi.yaml"
      via: "COPY api-spec/ for OpenAPI codegen during build"
      pattern: "COPY api-spec/"
---

<objective>
Integrate Google Cloud Run deployment infrastructure into main branch.

Purpose: The deployment files (Dockerfile, .dockerignore, DEPLOYMENT.md, application.yml PORT change) already exist on the `feature/google-cloud-run-deployment` branch. This plan brings them onto main, preserving the STIB API configuration added during v1.0 work.

Output: Deployment-ready main branch with Dockerfile, .dockerignore, DEPLOYMENT.md, and updated application.yml.
</objective>

<execution_context>
@/home/florent/.claude/get-shit-done/workflows/execute-plan.md
@/home/florent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deployment infrastructure files from feature branch</name>
  <files>Dockerfile, .dockerignore, DEPLOYMENT.md, backend/src/main/resources/application.yml</files>
  <action>
    Bring deployment files from `origin/feature/google-cloud-run-deployment` onto main:

    1. **Dockerfile** — Copy from branch using `git show origin/feature/google-cloud-run-deployment:Dockerfile`. This is a multi-stage build: build stage uses `eclipse-temurin:25-jdk` to run `./gradlew bootJar --no-daemon`, run stage uses `eclipse-temurin:25-jre` with just the JAR. The Dockerfile lives at repo root because the Gradle build needs access to `../api-spec/openapi.yaml`.

    2. **.dockerignore** — Copy from branch. Excludes `frontend/`, `.git/`, `backend/build/`, `backend/.gradle/`, and `*.md`.

    3. **DEPLOYMENT.md** — Copy from branch. Documents the full Cloud Run deployment process, prerequisites, troubleshooting. Update it to mention STIB_API_KEY must be set as an env var on the Cloud Run service (add a section about environment variables, noting the deploy command should include `--set-env-vars "STIB_API_KEY=your-key"`).

    4. **backend/src/main/resources/application.yml** — Change ONLY the server.port line from `port: 8080` to `port: ${PORT:8080}`. Keep ALL existing config (stib.api.base-url, stib.api.key, logging) intact. The branch version removed STIB config because it predates v1.0 — do NOT use the branch version directly.

    After creating files, verify the backend still compiles: `cd backend && ./gradlew compileJava`.
  </action>
  <verify>
    <automated>cd /home/florent/personal-dev/public-transports/transit && test -f Dockerfile && test -f .dockerignore && test -f DEPLOYMENT.md && grep -q '${PORT:8080}' backend/src/main/resources/application.yml && grep -q 'STIB_API_KEY' backend/src/main/resources/application.yml && cd backend && ./gradlew compileJava 2>&1 | tail -5</automated>
  </verify>
  <done>Dockerfile, .dockerignore, DEPLOYMENT.md exist at repo root. application.yml has PORT env var with 8080 fallback AND retains STIB API config. Backend compiles successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify deployment to Google Cloud Run</name>
  <action>
    Human deploys to Cloud Run and verifies the service works. Claude has prepared all deployment files — the user runs the actual gcloud commands since they require GCP authentication.
  </action>
  <verify>User confirms the deployed service responds correctly.</verify>
  <done>Cloud Run service is live and responding to API requests with STIB departure data.</done>
  <what-built>Deployment infrastructure files are now on main: Dockerfile (multi-stage build), .dockerignore, DEPLOYMENT.md, and updated application.yml with PORT env var support.</what-built>
  <how-to-verify>
    1. Review the files look correct: `git diff` to see all changes
    2. Optionally test Docker build locally: `docker build -t transit-backend .`
    3. Deploy to Cloud Run (from repo root):
       ```bash
       gcloud run deploy transit-planner-backend \
         --source . \
         --region europe-west1 \
         --allow-unauthenticated \
         --memory 512Mi \
         --cpu 1 \
         --min-instances 0 \
         --max-instances 1 \
         --port 8080 \
         --set-env-vars "STIB_API_KEY=your-key-here"
       ```
    4. Verify the deployed service responds:
       ```bash
       curl https://transit-planner-backend-621870148637.europe-west1.run.app/api/departures
       ```
  </how-to-verify>
  <resume-signal>Type "approved" once deployment is verified, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- Dockerfile at repo root builds a valid Spring Boot image
- .dockerignore keeps build context small (no frontend, no .git)
- application.yml reads PORT from environment, falls back to 8080, retains all STIB config
- DEPLOYMENT.md documents complete deployment process including STIB_API_KEY
- Backend compiles and tests pass
</verification>

<success_criteria>
- All four deployment files exist on main branch
- Backend compiles successfully with the application.yml change
- User confirms deployment to Cloud Run works (checkpoint)
</success_criteria>

<output>
After completion, create `.planning/phases/01-deploy-to-gcloud/01-01-SUMMARY.md`
</output>
