server:
  port: ${PORT:8080}

spring:
  application:
    name: transit-backend

stib:
  api:
    base-url: https://api-management-opendata-production.azure-api.net/api/datasets/stibmivb/rt/WaitingTimes
    key: ${STIB_API_KEY}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

logging:
  level:
    com.transit: DEBUG
    org.springframework.web: INFO
